import { unansweredIcon } from 'question-site/helpers/unanswered-icon';
import { module, test } from 'qunit';

module('Unit | Helper | unanswered icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unansweredIcon([42]);
  assert.ok(result);
});
